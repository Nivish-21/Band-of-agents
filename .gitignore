# Secrets — never commit
.env
agent_config.yaml

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# Local agent config (enables Gemini YOLO — keep local, never publish)
.gemini/

# OS
.DS_Store
